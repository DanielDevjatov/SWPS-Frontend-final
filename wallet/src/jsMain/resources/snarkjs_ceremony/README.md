## Compiling a Circuit
For a circuit called `CIRCUIT_NAME.circom`, call `./compile.sh CIRCUIT_NAME`.
It automatically compiles the circuit into the proper format.

## Adding a new Circuit
Create a .circom file in this directory and compile it. The program automatically scans the directory for all circuits.